<Window x:Class="Erp.View.Thesis.CustomButtons.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Erp.View.Thesis.CustomButtons"
        mc:Ignorable="d"
        Title="CustomMessageBox" Height="130" Width="350">
    <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>

            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding Message}" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Grid.Row="1" >
            <Button Content="Save Only" Click="SaveOnly_Click"/>
            <Button Content="Save and Upgrade" Click="SaveAndUpgrade_Click"/>
            <Button Content="Upgrade Only" Click="UpgradeOnly_Click"/>
        </StackPanel>
    </Grid>
</Window>
